# DevOS iOS 构建说明（免费 Apple ID + AltStore 方案）

本目录包含将 DevOS 手机版打包为 iOS 应用（.ipa）所需的全部工程文件。
核心思路：**GitHub 云端 macOS 构建 → 产出 .ipa → Windows 用 AltStore 签名安装到 iPhone**。
无需付费开发者账号（99 美元/年），使用免费 Apple ID 即可，7 天重签一次（AltStore 可自动续签）。

---

## 一、需要准备（约 15 分钟）

1. **GitHub 账号**（免费）：https://github.com 注册
2. **Apple ID**（免费）：https://appleid.apple.com 注册（已有可跳过）
3. 一台 **iPhone**
4. Windows 电脑上安装：
   - **iCloud for Windows**（AltStore 依赖，微软商店可装）或 iTunes
   - **AltStore**：https://altstore.io 下载 Windows 版（即 AltServer）

## 二、把工程推送到 GitHub（二选一）

### 方式 A：网页上传（简单，无需安装 Git）
1. 打开 https://github.com/new ，仓库名填 `devos-ios`，勾选 Public，创建
2. 进入仓库 → Add file → Upload files
3. 把本目录下所有文件拖进去（**注意 `.github` 文件夹也要传**，网页上传会保留子目录结构）
   - 需要传：`package.json`、`capacitor.config.json`、`www/`（3 个文件）、`.github/workflows/build-ios.yml`
4. Commit changes

### 方式 B：Git 命令行（需要安装 Git for Windows）
```bash
cd e:\GitHub\DevOS\devos-ios
git init
git add .
git commit -m "DevOS iOS build"
git branch -M main
git remote add origin https://github.com/你的用户名/devos-ios.git
git push -u origin main
```

## 三、云端构建（约 8 分钟，全自动）

1. 打开 GitHub 仓库 → **Actions** 标签 → 左侧 "Build DevOS iOS IPA" → 右侧 **Run workflow** → 绿色按钮确认
2. 等待构建完成（macOS 机器启动 + 编译，约 5-8 分钟）
3. 构建成功后，进入该次运行的页面 → 底部 **Artifacts** → 下载 **DevOS-ipa.zip**
4. 解压得到 `DevOS.ipa`

## 四、安装到 iPhone（约 5 分钟）

1. 手机连电脑 **USB 数据线**（首次需在手机弹窗选"信任此电脑"）
2. Windows 上启动 **AltServer**（托盘出现图标）
3. 用数据线连着的 iPhone 打开设置 → 通用 → 描述文件与设备管理 → 信任开发者 App（首次）
4. 回到 Windows：把 `DevOS.ipa` 拖到托盘 AltServer 图标上 → 选择你的 iPhone → 输入 **Apple ID 和密码**（这是 AltStore 官方机制，用于签名，不经过其他服务器）
5. 等几秒，iPhone 上出现 **DevOS** 图标即安装成功

## 五、7 天重签（续期）

- 免费签名 **7 天有效**，到期后 App 打不开
- 续期方法：iPhone 上打开 **AltStore** → 找到 DevOS → 点 **Refresh**（需手机与电脑同一 Wi-Fi，且 Windows 上 AltServer 正在运行）
- 想省心可后续升级付费开发者账号（99 美元/年，签名 1 年有效）

## 六、常见问题

| 问题 | 解决 |
|---|---|
| 构建失败（Capacitor 版本） | 检查 package.json 里 @capacitor 版本，GitHub Actions 日志会提示 |
| 下载的 .ipa 无法被 AltStore 识别 | 确认是从 Actions Artifacts 下载并解压出的 DevOS.ipa（不是 zip 内层嵌套） |
| AltServer 报 iCloud 未安装 | 安装 iCloud for Windows 并登录一次任意 Apple ID |
| 手机不弹"信任" | 设置 → 隐私与安全性 → 开发者模式 打开 |
| App 打开白屏 | 重新 `npx cap sync` 后重新构建，或检查 www 目录文件是否齐全 |

---

## 本目录结构

```
devos-ios/
├── package.json              # Capacitor 依赖
├── capacitor.config.json     # 应用配置（appId: com.devos.mobile）
├── www/                      # App 内置页面（手机版 Web 资源）
│   ├── index.html
│   ├── manifest.json
│   ├── sw.js
│   └── icon.png
└── .github/workflows/
    └── build-ios.yml         # macOS 云端构建工作流
```
